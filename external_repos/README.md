# External Repositories

This directory is for third-party references only.

Rules:

- Do not build project identity around these repositories.
- Do not edit checked-out references unless explicitly asked.
- Summarize useful mechanisms in `literature/` or `research/`.
- Keep core implementation in `src/` and project scripts in `tools/`.
