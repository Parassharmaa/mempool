# Agent Context

This directory stores reusable agent-facing project context.

- `skills/` contains task-specific workflows in `SKILL.md` format.

Project-wide always-on guidance remains in `AGENTS.md`. Skills should stay
focused and load only when relevant.
